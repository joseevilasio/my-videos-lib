[tool.poetry]
name = "api-myvideos"
version = "0.1.0"
description = "CRUD API"
authors = ["Jose Junior"]
license = "unlicensed"
readme = "README.md"
packages = [{include = "api_myvideos"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
